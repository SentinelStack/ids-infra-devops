# IDS Platform Backend runtime environment example.
# Copy to the target server as ids-platform-backend.env and fill real values there.

ENVIRONMENT=qa
SERVER_PORT=8082

# The current backend is minimal and does not require database or MISP secrets yet.
# Add service-specific runtime variables here when those features are implemented.
