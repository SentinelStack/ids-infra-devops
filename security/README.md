# Security

Security rules for operating the platform.

Initial focus:

- no committed secrets
- SSH key discipline
- HTTPS for exposed services
- least-privilege deployment user
- service processes should not run as root

