# QA Server

Recommended packages:

- Java runtime compatible with the backend
- Nginx
- curl
- systemd
- certbot, if using Let's Encrypt

Recommended base directory:

```text
/opt/ids-platform
```

Recommended service user:

```text
ids-backend
```

