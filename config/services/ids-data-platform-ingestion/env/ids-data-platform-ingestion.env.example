# Non-secret config for the data-platform ingestion service.
# All connections are local to the VPS. No secrets (backend + Kafka are local).

SERVER_PORT=8090
BACKEND_BASE_URL=http://localhost:8082
KAFKA_BOOTSTRAP=localhost:9092
